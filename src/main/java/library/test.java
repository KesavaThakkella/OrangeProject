package library;

public class test {

	public class calendar {
		/*
		 * //public static void selectdatebyjs(WebDriver driver, WebElement
		 * element, String dateval) {
		 * 
		 * //Javascript Executor js = (JavascriptExecutor) driver;
		 * //js.executeScript("arguments[0].setAttribute('value','" + dateval +
		 * "');", element);
		 * 
		 * }
		 */
	}
}